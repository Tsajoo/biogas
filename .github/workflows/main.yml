name: Build Unsigned IPA (for TrollStore)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-ipa:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter (3.19.6)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'
          cache: false   # disable cache to ensure the exact version is used

      - name: Install dependencies
        run: flutter pub get

      # Remove android folder to avoid Android embedding v2 error
      - name: Remove android folder
        run: rm -rf android

      # Generate iOS project (if missing)
      - name: Generate iOS project
        run: flutter create --platforms=ios .

      # Build unsigned iOS app
      - name: Build iOS app (unsigned)
        run: flutter build ios --release --no-codesign

      # Create IPA archive
      - name: Create IPA archive
        run: |
          mkdir -p Payload
          cp -r build/ios/iphoneos/Runner.app Payload/
          zip -r biogas.ipa Payload/

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: biogas-unsigned
          path: biogas.ipa
