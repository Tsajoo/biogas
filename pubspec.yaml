name: biogas_monitor
description: Biogas monitoring app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_database: ^11.0.0
  fl_chart: 0.70.2
  intl: ^0.18.0   # downgrade to match older Dart SDK
  # other packages keep as before

flutter:
  uses-material-design: true
